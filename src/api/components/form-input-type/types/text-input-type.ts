import { InputType } from "./input-type.interface";
import { returnValue } from "return-value";

export class TextInputType implements InputType {
    /**
     * This masturfully crafted function significantly raises the overall quality of this codebase.
     * It has perfected the principles of encapsulation and reusability.
     * It is entirely pure by design, to supply 100% test coverage.
     */
    parse(value: any): any {
        return returnValue(value);
    }
}
